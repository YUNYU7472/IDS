# 快速开始指南

## 🚀 5分钟快速启动

### 第一步：安装依赖

```bash
cd intrusion_detection_system
pip install -r requirements.txt
```

### 第二步：测试系统

```bash
python test_system.py
```

如果看到"所有测试通过"，说明系统已就绪！

### 第三步：启动服务

**Linux/Mac:**
```bash
./start.sh
```

**Windows:**
```bash
start.bat
```

**或者手动启动:**
```bash
cd backend
python main.py
```

### 第四步：访问Dashboard

打开浏览器访问：
```
http://localhost:8000/dashboard
```

---

## 📖 功能演示

### 1. 查看API文档

访问 `http://localhost:8000/docs` 查看自动生成的Swagger API文档

### 2. 启动联邦学习训练

在Dashboard中：
1. 设置客户端数量（建议5个）
2. 设置训练轮数（建议10轮）
3. 点击"开始训练"
4. 观察实时训练进度和准确率曲线

**预计时间：** 5-10分钟（取决于CPU性能）

### 3. 测试入侵检测

点击Dashboard上的"模拟检测"按钮，系统会：
- 生成随机流量特征
- 调用检测API
- 显示检测结果和置信度
- 更新统计图表

### 4. 使用Python调用API

```python
import requests
import numpy as np

# 生成测试特征
features = np.random.rand(122).tolist()

# 调用检测API
response = requests.post(
    'http://localhost:8000/api/detect',
    json={'features': features}
)

result = response.json()
print(f"检测结果: {result['prediction']}")
print(f"置信度: {result['confidence']:.2%}")
```

---

## 🔍 常见问题

### Q1: 启动时提示"数据文件未找到"

**解决方法：**
确保数据文件在正确位置：
```
Federated_Learning_Evaluation/
├── data/
│   ├── KDDTrain+.csv  ← 必须存在
│   └── KDDTest+.csv   ← 必须存在
└── intrusion_detection_system/
```

### Q2: 训练过程中出现内存不足

**解决方法：**
1. 减少客户端数量（如3个客户端）
2. 减少批次大小：编辑 `backend/config.py`，修改 `BATCH_SIZE = 16`

### Q3: 前端无法连接后端

**解决方法：**
1. 确认后端服务已启动（查看终端是否有"Application startup complete"）
2. 检查端口8000是否被占用
3. 修改 `frontend/dashboard.js` 中的 `API_BASE_URL`

### Q4: 导入模块失败

**解决方法：**
```bash
# 重新安装依赖
pip install -r requirements.txt --upgrade

# 如果使用conda
conda install pytorch scikit-learn pandas numpy
pip install fastapi uvicorn imbalanced-learn
```

---

## 📊 系统要求

### 最低配置
- CPU: 双核
- RAM: 4GB
- 磁盘: 500MB
- Python: 3.8+

### 推荐配置
- CPU: 四核或以上
- RAM: 8GB+
- GPU: 可选（加速训练）
- Python: 3.9+

---

## 🎯 下一步

完成Phase 1后，可以继续：

1. **Phase 2:** 实现WebSocket实时通信
2. **Phase 3:** 添加数据库支持
3. **Phase 4:** Docker容器化部署

查看 [README.md](README.md) 了解详细的开发计划。

---

## 💡 提示

1. **首次训练**需要加载和预处理数据，会比较慢
2. 训练完成后，模型会自动保存到 `models/` 目录
3. 可以直接使用已训练的模型进行检测，无需重新训练
4. Dashboard支持实时刷新，可以同时打开多个标签页

---

## 📞 获取帮助

- 查看完整文档：[README.md](README.md)
- API文档：http://localhost:8000/docs
- 检查系统状态：http://localhost:8000/health

有问题请提交Issue！
